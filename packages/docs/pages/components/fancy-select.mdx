import Head from 'next/head';

import { FancySelect2, Input } from '@cube/components';

import Example from '@components/Example';
import FancySelectCustomization from '@components/Examples/Select/FancySelectCustomization';
import { litteral } from '@components/Examples/utils';
import MDXPage from '@components/Layout/MDXPage';
import PropsList from '@components/PropsList';

<Head>
  <title>Fancy Select - Cube</title>
  <meta
    name="description"
    content="A custom select component that provides better design supported with react-virtualized to render large options lists."
  />
</Head>

## Import

```js
import { FancySelect2 } from '@cube/components';
```

## Usage

<Example>
  <FancySelect2
    options={[
      {
        id: 'asia',
        value: 'asia',
        text: 'Asia',
      },
      {
        id: 'afria',
        value: 'afria',
        text: 'Africa',
      },
      {
        id: 'europe',
        value: 'europe',
        text: 'Europe',
        disabled: true,
      },
      {
        id: 'americas',
        value: 'americas',
        text: 'Americas',
        children: [
          {
            id: 'north-america',
            value: 'north-america',
            text: 'North America',
          },
          {
            id: 'middle-america',
            value: 'middle-america',
            text: 'Middle America',
          },
          {
            id: 'south-america',
            value: 'south-america',
            text: 'South America',
          },
        ],
      },
    ]}
  />
</Example>

```jsx
<FancySelect
  options={[
    {
      id: 'asia',
      value: 'asia',
      text: 'Asia',
    },
    {
      id: 'afria',
      value: 'afria',
      text: 'Africa',
    },
    {
      id: 'europe',
      value: 'europe',
      text: 'Europe',
      disabled: true,
    },
    {
      id: 'americas',
      value: 'americas',
      text: 'Americas',
      children: [
        {
          id: 'north-america',
          value: 'north-america',
          text: 'North America',
        },
        {
          id: 'middle-america',
          value: 'middle-america',
          text: 'Middle America',
        },
        {
          id: 'south-america',
          value: 'south-america',
          text: 'South America',
        },
      ],
    },
  ]}
/>
```

## Customization

use `className` property to pass css classes to customize the `Input` component however you want.

<FancySelectCustomization />

```jsx
const people = [
  ...
  {
    id: '@redington',
    value: '@redington',
    avatar:
      'https://images.unsplash.com/photo-1531427186611-ecfd6d936c79?ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&ixlib=rb-1.2.1&auto=format&fit=facearea&facepad=2&w=256&h=256&q=80',
    text: 'Wade Redington',
    username: '@redington',
  },
  ...
];

<FancySelect
  placeholder="Please select a user account"
  options={people}
  inputRenderer={({ option, ref, ...props }) => {
    const selected = option as any;
    return (
      <FancySelect.Input
        ref={ref as any}
        {...props}
      >
        {selected ? (
          <Avatar
            image={selected.avatar}
            alt={selected.text}
            name={selected.text}
            subtext={selected.username}
          />
        ) : (
          <Avatar
            alt="Select a user"
            name="Select a user"
            subtext="Select a user from the available users list"
          />
        )}
      </FancySelect.Input>
    );
  }}
  optionRenderer={(option, props) => {
    const person = option as any;
    return (
      <FancySelect.Option
        {...props}
        className="justify-start space-x-3 border border-gray-200 border-dotted"
      >
        <Avatar
          image={person.avatar}
          alt={person.text}
          name={person.text}
          subtext={person.username}
        />
      </FancySelect.Option>
    );
  }}
/>
```

## Props

The `Input` accepts any prop that a normal html `input` can have. Plus the `error` prop.

<PropsList
  props={[
    {
      name: 'error',
      type: 'boolean',
      description: 'Indicates if input field has an invalid state.',
      default: 'false',
    },
  ]}
/>

export default ({ children }) => (
  <MDXPage
    title="Fancy Select"
    page="fancy-select"
    description="A custom select component that provides better design supported with react-virtualized to render large options lists."
  >
    {children}
  </MDXPage>
);
