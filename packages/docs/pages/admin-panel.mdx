import {
  ChevronDownIcon,
  DuplicateIcon,
  PencilAltIcon,
  TrashIcon,
} from '@heroicons/react/outline';
import Head from 'next/head';

import { Button, Dropdown } from '@cube/components';

import Example from '@components/Example';
import MDXPage from '@components/Layout/MDXPage';
import TextItem from '@components/TextItem';

<Head>
  <title>Admin Panel - Cube</title>
  <meta
    name="description"
    description="Supercharge your next project with a powerful and beautiful admin panel."
  />
</Head>

<div className="space-x-3">
  <h3>We're still working on it .....</h3>
  <p>
    Thank you for showing your interenest in cube and the admin panel. we're
    still working on this one and we'll make sure to get it done as soon as we
    can.
  </p>
</div>

export default ({ children }) => (
  <MDXPage
    title="Admin Panel"
    page="admin-panel"
    description="Supercharge your next project with a powerful and beautiful admin panel."
  >
    {children}
  </MDXPage>
);
