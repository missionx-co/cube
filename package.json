{
  "name": "cube",
  "version": "0.0.1",
  "description": "Design System, UI Kit and an Admin dashboard built with React and Tailwind CSS. Based on Untitled UI",
  "private": true,
  "scripts": {
    "components:watch": "yarn workspace @cube/components dev",
    "components:types": "yarn workspace @cube/components types",
    "docs:watch": "yarn workspace @cube/docs dev",
    "types": "yarn workspace @cube/components types && yarn workspace @cube/foundation types",
    "build": "yarn workspace @cube/components build && yarn workspace @cube/foundation build",
    "clean": "yarn workspaces run clean"
  },
  "workspaces": [
    "packages/*"
  ]
}
